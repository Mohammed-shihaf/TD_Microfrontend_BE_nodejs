apiVersion: apps/v1
kind: Deployment
metadata:
  name: microfrontend-be-nodejs
  labels:
    app: microfrontend-be-nodejs
spec:
  replicas: 2
  selector:
    matchLabels:
      app: microfrontend-be-nodejs
  template:
    metadata:
      labels:
        app: microfrontend-be-nodejs
    spec:
      containers:
        - name: app
          image: microfrontend-be-nodejs:18
          ports:
            - containerPort: 3000
          readinessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: microfrontend-be-nodejs
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
  selector:
    app: microfrontend-be-nodejs
